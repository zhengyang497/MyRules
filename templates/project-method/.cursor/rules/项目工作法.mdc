---
description: 项目工作法守门：总账领号、一卡一页、出口成能力、审计守则
alwaysApply: true
---

# 项目工作法

完整说明见 `docs/方法/项目工作法.md`。改代码或改文档前先读技能 `project-method`。

## 看板与领号
- 还要被找回来的工作，没有看板项（新建或点出现有 `item-N`）之前不动手。一行能结束、不值得交代的小改可以不登记；拿不准就问人要不要登记，不要自行开卡。登记用 `npm run board -- add` / `patch`。
- 登记只要标题和几句说明，写进这张卡，不强制当时写落点，不预置三节，不另挂指针。不要把卡片目录或讨论文稿当收件箱。
- `docs/看板/items/item-N.md` 的页眉字段（编号、状态、focus、落点等）只经 `node scripts/board.mjs` 修改，禁止手改页眉；卡片正文按普通 markdown 改。

## 承诺与能力
- 现行承诺只写在 `docs/能力/<功能名>/<功能名>设计目标.md` 的文首，且只写现在就该能对质的**目的**。拿掉这句产品要的那件事变了，才进表。字段、路径、切篇、prompt 纪律不是能力，不进文首。
- 走查记录、调试日志、某日现场、尚未做成的句子与做法细节只进对应看板卡，不得写入设计目标文首。
- 对话里出现的新目的不算落地；写进对应文首（必要时改 `.myrules-context.md` 那句目的）才算。
- `docs/设计目标检查清单.md` 禁止人手改；该表只由模型在改了文首后同一轮逐字同步，不记未落地。

## 收尾与审计
- 标完成时问有没有拿掉就会换目的、现在就该能对质的句子，并在卡片正文写下单独一行「收尾：有」或「收尾：没有」；没有长期目的就只关卡。
- 人说审计、目标审计或项目目标审计：按技能流程五三阶段做（收尾 → 清单矛盾 → 对照代码产物），缺收尾行就停。
- 目的调整、借鉴外部系统、开始写代码、先登记、不知道记在哪、验收算不算做成：先按技能 `project-method` 的流程做，再动还要被找回来的代码。
