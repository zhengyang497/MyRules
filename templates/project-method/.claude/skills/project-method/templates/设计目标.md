# <功能名>设计目标

> 目标只认文首。文首写目标，不写做法。目标是决定走向的拍板（人在场景中必须能断定什么，或业务硬边界）。换工程做法仍成立的实施手段（字段、路径、切篇、prompt 纪律）不进文首。每条带状态：口号 / 做了一截 / 已做成。状态标针对这句的动作做到哪，不是抽查或审计结论。项目列可空。改标准用原地替换，不要在文末追加日记。走查、调试和做法细节写进对应看板卡，不写进文首。口号留在文首。

## 目的

（谁在什么情况下能做成哪件具体的事。一句。从自己的需求写，不从别人的代码推。可以先是口号。）

## 目标

| ID | 目标 | 状态 | 对比时看什么 | 验证方式 | 项目 |
|---|---|---|---|---|---|
| X1 | （拍板定夺：人在场景中能断定什么，或业务硬边界。换工程做法依然成立的不写。不写字段/路径/切篇） | 口号 | （拿什么、看哪、怎样算没做成。口号阶段可写尚未做成） | 人抽查（实质） 或 测试名 | （可空） |
| X2 | （业务硬边界：系统绝不能越过的底线/防污染边界。常规校验不进表） | 做了一截 | | | |

## 为什么这样

（取舍理由。借鉴过别人则写：对方怎么做、为什么、我们认哪几项、为什么砍。理由指向本项目目的。）

## 边界

（明确不做的、不保证的。）
