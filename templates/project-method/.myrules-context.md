# 项目上下文

当前目的：（用一句话写本项目现在要做成的事。）

目标看 [`docs/设计目标检查清单.md`](docs/设计目标检查清单.md)，含口号和尚未做成的。工作现场是改进清单看板（`npm run board` / `npm run board:server`）。通用红线在 MyRules 同步下来的行为规则里。

目的变了，先改这一句和对应那篇《功能名设计目标》的文首，再改代码。对话里说清楚了不算落地。口号进册要人点头。
