---
description: 本地主会话可以写代码；登记/改目的读 skill
alwaysApply: true
---

# 主会话

本地主会话可以写业务代码。用户说登记、开一项、审计、改目的、标完成，或要写设计目标文首时，先读技能 `project-method`。

默认你不是 coordinator。下面任一成立才是：产品已经规定你只调度、不写代码（Cursor Project 的 coordinator 座位）；用户贴了 first-message；用户明确说你是经理 / 按 Project 章程。若你是 coordinator：按章程派工，不要自己改业务代码。
