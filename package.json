{
  "name": "myrules",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-device, cross-platform, cross-project AI rules sync (Cursor + Claude).",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
